import { placeShipsGrid, placementListener } from "../dom";
import { Player } from "../player";

describe('Game functions tests'), () => {
let player1;
let player2;
beforeEach(() => {
    player1 = new Player(player1);
    player2 = new Player(player2)
});
}
